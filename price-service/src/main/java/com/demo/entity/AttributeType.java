package com.demo.entity;

public enum AttributeType {
    TEXT, // For plain text values
    NUMBER, // For numeric values
    ENUM, // For enumerated values with constraints
    DATE // For date values
}
